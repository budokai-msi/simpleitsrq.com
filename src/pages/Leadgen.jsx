import { useDeferredValue, useEffect, useMemo, useState } from "react";
import "../styles/leadgen.css";
import "../styles/leadgen-product.css";
import { Link } from "../lib/Link";
import { ArrowRight, ChevronDown, Mail, Search } from "lucide-react";
import { useSEO, SITE_URL } from "../lib/seo";
import { trackEvent } from "../lib/analytics.js";
import { csrfFetch } from "../lib/csrf";
import LeadgenWorkspace from "../components/leadgen/LeadgenWorkspace";

const LEADGEN_PROMO_CODE = "LAUNCH20";
const LEADGEN_STRIPE_LINKS = {
  growth: { monthly: "https://buy.stripe.com/8x2cMYaAX3qg648aUlak01y", annual: "https://buy.stripe.com/9B65kwgZl4uk9gk7I9ak01z" },
  pro: { monthly: "https://buy.stripe.com/14A8wI5gDbWM0JO9Qhak01A", annual: "https://buy.stripe.com/4gM28kaAX1i8eAE0fHak01B" },
};
const PUBLIC_NICHES = ["All","Healthcare","Trades","Professional Services","Automotive","Hospitality","Personal Services","Retail","Food & Drink","Education","Real Estate","Cleaning & Maintenance","Media & Creative","Recreation"];
const SCAN_LIMIT = 80;
function stripeSafeParam(value,fallback="leadgen"){const safe=String(value??"").toLowerCase().replace(/&/g,"and").replace(/[^a-z0-9_-]+/g,"_").replace(/^_+|_+$/g,"").slice(0,80);return safe||fallback;}
function leadgenCheckoutReference({tierId="growth",billing="monthly",source="leadgen",checkoutContext={}}={}){const parts=["lg",stripeSafeParam(tierId),stripeSafeParam(billing)];const zip=String(checkoutContext.zip||"").replace(/\D/g,"").slice(0,5);if(zip)parts.push("zip",zip);if(checkoutContext.niche&&checkoutContext.niche!=="All")parts.push("niche",stripeSafeParam(checkoutContext.niche));parts.push("src",stripeSafeParam(source));return parts.join("_").slice(0,150);}
function withLeadgenCheckoutParams(url,options={}){if(!url)return"";try{const next=new URL(url);if(!next.searchParams.has("prefilled_promo_code"))next.searchParams.set("prefilled_promo_code",LEADGEN_PROMO_CODE);const ref=leadgenCheckoutReference(options);next.searchParams.set("client_reference_id",ref);next.searchParams.set("utm_source","simpleitsrq_com");next.searchParams.set("utm_medium","leadgen");next.searchParams.set("utm_campaign","leadgen_checkout");return next.toString();}catch{return url;}}
function csvCell(value){return `"${String(value??"").replace(/"/g,'""')}"`;}
function downloadCsv(filename,rows){const headers=["status","name","industry","sub_industry","address","city","state","zip","website","phone","email","opportunity_score","opportunity_grade","source_url"];const lines=[headers.map(csvCell).join(","),...rows.map(r=>headers.map(k=>csvCell(k==="email"?(r.email||r.emails?.[0]?.email||""):r[k])).join(","))];const blob=new Blob([lines.join("\n")],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);}

function LeadgenScanApp(){
  const [zip,setZip]=useState(""); const [niche,setNiche]=useState("All"); const [scan,setScan]=useState(null); const [review,setReview]=useState({}); const [busy,setBusy]=useState(false); const [err,setErr]=useState(""); const [searchTerm,setSearchTerm]=useState(""); const [destinations,setDestinations]=useState([]); const [pushTarget,setPushTarget]=useState(""); const [pushBusy,setPushBusy]=useState(false); const [pushMsg,setPushMsg]=useState(null); const [extractedEmails,setExtractedEmails]=useState({}); const [extracting,setExtracting]=useState(false); const [extractMsg,setExtractMsg]=useState(null); const [saveMsg,setSaveMsg]=useState(null); const [openGroups,setOpenGroups]=useState({});
  const deferredSearchTerm=useDeferredValue(searchTerm); const validZip=/^\d{5}$/.test(zip); const rows=scan?.rows||[];
  const reviewedRows=useMemo(()=>rows.map((row,index)=>({...row,status:review[index]||"keep",__scanIndex:index})),[rows,review]);
  const visibleRows=useMemo(()=>reviewedRows.filter(row=>!deferredSearchTerm.trim()||[row.name,row.city,row.industry_group,row.sub_industry,row.website,row.phone,...(row.opportunity_reasons||[])].filter(Boolean).join(" ").toLowerCase().includes(deferredSearchTerm.trim().toLowerCase())),[reviewedRows,deferredSearchTerm]);
  const groupedRows=useMemo(()=>{const groups=new Map();for(const row of visibleRows){const industry=row.industry_group||row.industry||"Other";const sub=row.sub_industry||"Other";if(!groups.has(industry))groups.set(industry,{name:industry,rows:[],subs:new Map(),withWebsite:0,withPhone:0,scoreTotal:0});const group=groups.get(industry);group.rows.push(row);if(row.website)group.withWebsite++;if(row.phone)group.withPhone++;group.scoreTotal+=Number(row.opportunity_score||0);if(!group.subs.has(sub))group.subs.set(sub,[]);group.subs.get(sub).push(row);}return Array.from(groups.values()).map(g=>({...g,avgScore:g.rows.length?Math.round(g.scoreTotal/g.rows.length):0})).sort((a,b)=>b.rows.length-a.rows.length||a.name.localeCompare(b.name));},[visibleRows]);
  const kept=reviewedRows.filter(r=>r.status==="keep"); const bestEmail=r=>r.email||r.emails?.[0]?.email||extractedEmails[r.website]||""; const keptWithEmail=kept.filter(r=>bestEmail(r)); const insights=scan?.market_insights||null;
  useEffect(()=>{fetch("/api/leadgen-integrations",{credentials:"same-origin",headers:{Accept:"application/json"}}).then(r=>r.ok?r.json():null).then(j=>{if(j?.integrations?.length){setDestinations(j.integrations);setPushTarget(String(j.integrations[0].id));}}).catch(()=>{});},[]);
  useEffect(()=>{if(!scan||!groupedRows.length)return;setOpenGroups(current=>{if(Object.keys(current).length)return current;const next={};groupedRows.slice(0,3).forEach(group=>{next[group.name]=true;});return next;});},[scan,groupedRows]);
  const runScan=async()=>{if(!validZip||busy)return;setBusy(true);setErr("");setScan(null);setReview({});setOpenGroups({});try{const res=await csrfFetch("/api/leadgen",{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify({zip,niche,limit:SCAN_LIMIT})});const data=await res.json().catch(()=>({}));if(!res.ok||data.ok===false)throw new Error(data.message||data.error||`HTTP ${res.status}`);setScan(data);trackEvent("search",{search_term:`${zip}:${niche}`,source:"leadgen_scanner",result_count:Number(data.matched||data.rows?.length||0)});}catch(e){setErr(e.message||"Scan failed.");}finally{setBusy(false);}};
  const selectBest=()=>{const next={};rows.forEach((r,i)=>{next[i]=Number(r.opportunity_score||0)>=65?"keep":"reject";});setReview(next);};
  const findEmails=async()=>{const targets=kept.filter(r=>r.website&&!bestEmail(r)).slice(0,30);if(!targets.length)return setExtractMsg({ok:true,text:"Selected leads already have emails or no crawlable websites."});setExtracting(true);setExtractMsg(null);const found={...extractedEmails};let count=0;try{for(let i=0;i<targets.length;i+=10){const batch=targets.slice(i,i+10);const res=await csrfFetch("/api/leadgen-emails",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({domains:batch.map(r=>r.website)})});const j=await res.json().catch(()=>({}));if(!res.ok)throw new Error(j.message||j.error||`HTTP ${res.status}`);(j.results||[]).forEach((result,idx)=>{const email=result.emails?.[0]?.email;if(email){found[batch[idx].website]=email;count++;}});}setExtractedEmails(found);setExtractMsg({ok:true,text:`Found ${count} additional email${count===1?"":"s"}.`});}catch(e){setExtractMsg({ok:false,text:e.message||"Email enrichment failed."});}finally{setExtracting(false);}};
  const pushSelected=async()=>{if(!pushTarget||!kept.length)return;setPushBusy(true);setPushMsg(null);try{const leads=kept.map(r=>({...r,email:bestEmail(r)||undefined}));const res=await csrfFetch("/api/leadgen-integrations?action=push",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:Number(pushTarget),leads})});const j=await res.json().catch(()=>({}));if(!res.ok||j.ok===false)throw new Error(j.message||j.error||`HTTP ${res.status}`);setPushMsg({ok:true,text:`Pushed ${j.sent??leads.length} lead${(j.sent??leads.length)===1?"":"s"}.`});}catch(e){setPushMsg({ok:false,text:e.message||"CRM push failed."});}finally{setPushBusy(false);}};
  const saveMarket=async()=>{if(!validZip)return;setSaveMsg(null);try{const res=await csrfFetch("/api/leadgen-workspace?action=save-market",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:`${niche} ${zip}`,zip,industry_group:niche==="All"?null:niche,schedule:"weekly"})});const j=await res.json().catch(()=>({}));if(!res.ok||j.ok===false)throw new Error(j.message||j.error||`HTTP ${res.status}`);setSaveMsg({ok:true,text:"Saved as a weekly monitored market."});}catch(e){setSaveMsg({ok:false,text:e.message||"Could not save market."});}};
  const toggleGroup=name=>setOpenGroups(current=>({...current,[name]:!current[name]}));
  const stage=!scan?0:kept.length===0?1:keptWithEmail.length<kept.length?2:3; const workflow=[["1. Discover","Choose a market and industry."],["2. Qualify","Keep the businesses worth pursuing."],["3. Enrich","Add reachable emails and contact data."],["4. Sync","Export or push into your revenue stack."]];
  return <section className="leadgen-app-shell" aria-label="Leadgen local market scanner">
    {scan&&kept.length?<div className="leadgen-selbar"><span className="leadgen-selbar__count"><strong>{kept.length}</strong> selected</span><div className="leadgen-selbar__actions"><button type="button" className="btn btn-secondary btn-sm" onClick={()=>downloadCsv(`leadgen-${zip}.csv`,kept)}>Export CSV</button>{destinations.length?<select value={pushTarget} onChange={e=>setPushTarget(e.target.value)}>{destinations.map(d=><option key={d.id} value={d.id}>{d.label||d.kind}</option>)}</select>:null}{destinations.length?<button type="button" className="btn btn-primary btn-sm" onClick={pushSelected} disabled={pushBusy}>{pushBusy?"Pushing…":"Push to CRM"}</button>:<Link to="/portal/leadgen" className="btn btn-primary btn-sm">Connect CRM</Link>}</div></div>:null}
    <div className="leadgen-app-panel leadgen-app-panel--control"><div className="leadgen-app-topline"><span className="leadgen-app-live"><span/> Live local-market data engine</span><Link to="/portal/leadgen" className="leadgen-app-portal-link">Campaign workspace</Link></div><div className="leadgen-app-title"><h2 className="title-2">Build a qualified local pipeline, not another spreadsheet.</h2><p>Discover businesses, qualify them, enrich contact data, then send the final list into your CRM or automation stack.</p></div>
    <div className="leadgen-product-steps">{workflow.map(([title,body],i)=><div key={title} className={`leadgen-product-step${i===stage?" is-active":""}`}><strong>{title}</strong><span>{body}</span></div>)}</div>
    <div className="leadgen-scan-card"><div className="leadgen-app-controls leadgen-app-controls--primary"><label><span>ZIP code</span><input inputMode="numeric" value={zip} onChange={e=>setZip(e.target.value.replace(/\D/g,"").slice(0,5))} placeholder="34236"/></label><label><span>Industry</span><select value={niche} onChange={e=>setNiche(e.target.value)}>{PUBLIC_NICHES.map(n=><option key={n}>{n}</option>)}</select></label><button type="button" className="btn btn-primary" onClick={runScan} disabled={!validZip||busy}><Search size={16}/>{busy?"Scanning…":"Run market scan"}</button></div>{validZip?<div className="leadgen-product-save"><button type="button" className="btn btn-secondary btn-sm" onClick={saveMarket}>Save + monitor weekly</button>{saveMsg?<span className={saveMsg.ok?"":"is-error"}>{saveMsg.text}</span>:null}</div>:null}{err?<p className="form-error" role="alert">{err}</p>:null}</div>
    {scan?<>{insights?<div className="leadgen-intel-strip" aria-label="Market intelligence"><article><strong>{insights.total}</strong><span>businesses mapped</span></article><article><strong>{insights.contactable_rate}%</strong><span>contactable</span></article><article><strong>{insights.independent_rate}%</strong><span>likely independent</span></article><article><strong>{insights.digital_gap_count}</strong><span>digital gaps</span></article><article><strong>{insights.high_opportunity_count}</strong><span>B-grade or better</span></article>{insights.top_industry?<article className="is-wide"><strong>{insights.top_industry.name}</strong><span>largest category · {insights.top_industry.share}% of market · avg score {insights.top_industry.avg_score}</span></article>:null}</div>:null}<div className="leadgen-product-toolbar"><label><span>Search results</span><input value={searchTerm} onChange={e=>setSearchTerm(e.target.value)} placeholder="Company, city, industry, signal…"/></label><button type="button" className="btn btn-secondary btn-sm" onClick={selectBest}>Select B+ prospects</button><button type="button" className="btn btn-secondary btn-sm" onClick={findEmails} disabled={extracting}>{extracting?"Enriching…":"Find missing emails"}</button></div>{extractMsg?<p className={extractMsg.ok?"":"form-error"}>{extractMsg.text}</p>:null}{pushMsg?<p className={pushMsg.ok?"":"form-error"}>{pushMsg.text}</p>:null}<div className="leadgen-category-stack">{groupedRows.map(group=>{const open=openGroups[group.name]!==false&&!!openGroups[group.name];return <section className={`leadgen-category${open?" is-open":""}`} key={group.name}><button type="button" className="leadgen-category__toggle" onClick={()=>toggleGroup(group.name)} aria-expanded={open}><div><strong>{group.name}</strong><span>{group.rows.length} businesses · {group.withWebsite} websites · {group.withPhone} phones · avg score {group.avgScore}</span></div><ChevronDown size={18} aria-hidden="true"/></button>{open?<div className="leadgen-category__body">{Array.from(group.subs.entries()).sort((a,b)=>b[1].length-a[1].length||a[0].localeCompare(b[0])).map(([sub,subRows])=><div className="leadgen-subcategory" key={sub}><div className="leadgen-subcategory__head"><strong>{sub}</strong><span>{subRows.length}</span></div><div className="leadgen-results-list">{subRows.map(row=><article className={`leadgen-result-row is-${row.status}`} key={row.source_id||row.__scanIndex}><label className="leadgen-product-check"><input type="checkbox" checked={row.status==="keep"} onChange={e=>setReview(cur=>({...cur,[row.__scanIndex]:e.target.checked?"keep":"reject"}))}/></label><div className="leadgen-result-row__main"><div className="leadgen-prospect-title"><strong>{row.name}</strong><span className={`leadgen-score grade-${String(row.opportunity_grade||"D").toLowerCase()}`}>{row.opportunity_grade} · {row.opportunity_score}</span></div><span>{[row.city,row.zip].filter(Boolean).join(" · ")}</span><small className="leadgen-prospect-reasons">{(row.opportunity_reasons||[]).slice(0,3).join(" · ")}</small></div><div className="leadgen-result-row__meta">{row.website?<a href={row.website} target="_blank" rel="noopener noreferrer">{new URL(row.website).hostname.replace(/^www\./,"")}</a>:<span>No website</span>}{bestEmail(row)?<a className="leadgen-result-row__email" href={`mailto:${bestEmail(row)}`}><Mail size={12}/>{bestEmail(row)}</a>:<span>Email not enriched</span>}{row.phone?<span>{row.phone}</span>:<span>Phone missing</span>}</div></article>)}</div></div>)}</div>:null}</section>})}{!groupedRows.length?<p className="leadgen-category-empty">No businesses match the current search.</p>:null}</div></>:null}
    </div>
  </section>;
}

export default function Leadgen(){useSEO({title:"Local Lead Generation & CRM Sync | Simple IT SRQ",description:"Discover local businesses, score and qualify prospects, monitor saved markets, enrich contacts, and sync attributable pipeline into your CRM.",canonical:`${SITE_URL}/leadgen`,image:`${SITE_URL}/og-image.png`});return <main id="main" className="leadgen-public"><div className="container leadgen-product-page"><LeadgenScanApp/><LeadgenWorkspace/><section className="leadgen-product-upgrade"><div><span className="eyebrow">Built for revenue teams</span><h2>Turn recurring local-market intelligence into pipeline.</h2><p>Saved markets, scoring, CRM sync, campaign history, suppression, attribution and health monitoring become more valuable as your team keeps using them.</p></div><a className="btn btn-primary" href={withLeadgenCheckoutParams(LEADGEN_STRIPE_LINKS.pro.monthly,{tierId:"pro",source:"leadgen_workspace"})}>Upgrade to Pro <ArrowRight size={16}/></a></section></div></main>;}
