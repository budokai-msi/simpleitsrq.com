// Digital products catalog. Currently empty — all prior PDF/spreadsheet
// products were retired Nov 2026 pending a single new offering: an
// automated lead-gen tool now under development. Pages that depend on
// this array (Store.jsx, ProductDetail.jsx, StoreCrossSell.jsx,
// GlossaryEntry.jsx) all gracefully render an empty state when the
// array is empty — no other code changes required to ship "no
// products" mode.
export const products = [];
